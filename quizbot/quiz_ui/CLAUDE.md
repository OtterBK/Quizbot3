# quizbot/quiz_ui/

Discord UI 화면들. `components/`(→ `components/CLAUDE.md`)에 버튼/모달/셀렉트메뉴 정의가 있고, 이 디렉터리 파일들은 그 컴포넌트를 조합한 화면(embed+components) 클래스.

## 프레임워크 (`common-ui.js`, `ui-system-core.js`)

- 모든 화면은 `QuizbotUI`(단순 메뉴) 또는 `QuizBotControlComponentUI`(페이지네이션 있는 목록, `prev`/`next`/`request_modal_page_jump` 처리 내장) 상속.
- `onInteractionCreate(interaction)`가 `new SomeUI(...)`를 반환하면 자동으로 화면 전환 + 이전 화면이 `prev_ui_stack`에 쌓임(뒤로가기용). `this`를 반환하면 같은 화면 유지하며 `update()`만. `undefined`면 아무 것도 안 함.
- `'back'` customId(컴포넌트: `only_back_comp`)는 `UIHolder`가 전역으로 가로채 자동 처리 — 개별 화면이 구현할 필요 없음.
- `UIHolder`: `UI_HOLDER_TYPE.PUBLIC`(길드 채널, `holder_id=guild_id`) / `PRIVATE`(DM, `holder_id=user_id`). `ui-system-core.js`의 `createXXXUIHolder` 함수들이 진입점(`createMainUIHolder`/`createQuizToolUIHolder`/`createAdminPanelUIHolder`).
- **`bot.js`의 "Public UI 소유자 제한"**: `uiHolder.on(...)`을 부르기 전에, PUBLIC UI(길드 채널)에서 `interaction.user.id != uiHolder.getOwnerId()`면(=UI를 만든 사람이 아니면) `uiHolder.on()`을 아예 호출하지 않고 바로 "해당 UI를 생성한 OOO님만이 조작할 수 있어요" 안내로 막아버림 — 각 화면의 `onInteractionCreate`에 도달하기도 전에 걸리는 **화면 코드보다 앞선 전역 게이트**. 예외는 멀티플레이 세션(로비 단계, 참가자 전원이 상호작용해야 하니까)만 원래 있었음. **`PUBLIC_UI_OWNER_CHECK_EXEMPT_PREFIXES`(2026-08-18 신설, `docs/plans/QUIZ_BASKET_PRESET_UI_PLAN.md`)**: "소유자 외 다른 유저도 참여 가능"하게 설계된 기능(퀴즈함 관리+프리셋의 `basket_manage_`/`modal_basket_preset_` 접두사)이 이 게이트에 걸려 애초에 시작도 못 했던 설계 미스를 실사용 중 발견 — customId 접두사 화이트리스트를 만들어 이 게이트만 우회하게 함(화면 진입 자체를 막지 않을 뿐, 액션별 세부 권한(예: 방장만 가능한 액션)은 각 기능 자체가 별도로 판단 — `basket-manage-flow.ts`의 `room_owner` 체크 참고). **새 기능을 PUBLIC UI 화면 안에 만들 때 "소유자 아닌 사람도 눌러야 하는 버튼"이 있으면 이 화이트리스트에 추가하는 걸 잊지 말 것** — 안 그러면 화면 코드가 완벽해도 애초에 인터랙션이 도달하지 않는다.
- `onAwaked()`: 자식 화면에서 뒤로가기로 돌아왔을 때 호출됨(선택 개수/태그 텍스트 재계산 등에 씀).
- **주의**: `interaction.user.send(...)`(순수 DM 전송)에 `flags: MessageFlags.Ephemeral`을 줘도 아무 효과 없음(`flags`는 `interaction.reply`/`deferReply`/`followUp`같은 인터랙션 응답 전용) — 에페메럴로 보내려면 반드시 `interaction.reply(...)`(이미 reply를 소비한 인터랙션이면 `interaction.followUp(...)`) 사용할 것 (2026-08-08, B-2 피드백 1번에서 4곳 발견해 수정).
- **주의**: `SelectUIModeUI`/`MainUI` 등 "`return new XXXUI()`"로 화면을 전환하는 대부분의 `onInteractionCreate`는 새 화면을 `base_interaction.editReply()`(PUBLIC)/`base_message.edit()`(PRIVATE)로 그리므로, 정작 그 순간 클릭된 인터랙션 자체는 명시적으로 reply되지 않는 게 정상 동작(`bot.js` 전역 fallback이 `deferUpdate()`로 조용히 ack해줌) — 여기에 `interaction.explicit_replied`를 안 세웠다고 "버튼이 안 눌린다"고 오해하지 말 것(2026-08-13, uiHolder/quiz_session이 찾아졌는데도 fallback이 "만료됐어요" 안내를 잘못 띄우던 회귀를 고치며 이 동작을 다시 확인함 — 상세는 `docs/COMPLETED_WORK_LOG.md` 2026-08-13 항목).
- `createMainUIHolder`(`ui-system-core.ts`)는 길드에 이미 다른 유저의 화면(`MainUI`든 `WebHandoffUI`든 무관)이 떠 있으면 "권한 가져오기" 하이재킹 방어 안내(`force_take_comp`)를 띄운다(2026-08-13 일반화 — 원래는 `WebHandoffUI`일 때만 가드가 걸려서, 디스코드 UI 사용 중엔 다른 유저가 `/퀴즈`를 입력하면 확인 없이 곧장 화면을 가로챌 수 있었음). **요청자가 그 화면의 소유자 본인이면 이 가드를 건너뛰고** 새 `SelectUIModeUI`를 띄운다 — 본인이 다시 `/퀴즈`를 입력해 트랙을 바꾸는 건 하이재킹이 아니라 정상 요청이기 때문.
- `bot.js`의 `handle_ui_force_take`(2026-08-13, 예전 이름 `handle_web_handoff_force_take`)는 다른 유저가 "권한 가져오기"(`ui_force_take` customId) 버튼을 눌렀을 때 처리한다 — 이전 홀더를 `free()`(웹 세션이 있었다면 `UIHolder.free()`가 알아서 release IPC까지 보냄)한 뒤 `createMainUIHolder`를 그대로 재사용해 이 버튼 인터랙션으로 새 `SelectUIModeUI`를 띄운다. 예전엔 웹 세션 토큰을 IPC `force_take` 액션으로 미리 새로 발급받아 곧장 `WebHandoffUI`로 진입시켰지만, 디스코드 UI 사용 중에도 이 버튼이 뜨게 되면서 "웹 세션"이라는 특정 개념에 안 묶이도록 단순화됨.

## 진입/관리자 (직접 다뤄본 파일)

- **`main-ui.js`** — 메인 메뉴(`MainUI`). 죽은 코드였던 `loadVersionInfo()`(설정에서 이미 비활성화된 기능)는 삭제됨.
- **`quiz-tool-guide-ui.js`** — "퀴즈 만들기" 안내 화면, 정적 안내문 + 뒤로가기만.
- **`note-select-ui.js`** — 공지/패치노트 목록. 패치노트(`patch_note_contents`)는 실제로는 채워지지 않는
  죽은 기능(2026-08-12 확인, "더 이상 안 씀"이 사용자 확인 방침 — 고칠 계획 없음). 목록 로딩 로직은
  `managers/notice_manager.ts`의 `loadNoticeList`로 추출됨(나머지 화면 웹 포팅, 아래 참고).
- **`user-quiz-list-ui.js`** — 내 퀴즈 목록(`UserQuizListUI`). 생성자 2번째 인자 `show_all_quizzes`(기본 false) — true면 전체 유저 퀴즈 조회(관리자 패널 전용). `show_all_quizzes=false`로 호출되면 자기 퀴즈만 봄. `/퀴즈만들기`(`createQuizToolUIHolder`)의 최초 화면은 2026-08-11부터 이 UI가 아니라 `QuizEditSelectUIModeUI`(아래) — 디스코드 UI 선택 시 이 UI로 진입(동작 동일).
- **`user-quiz-info.ui.js`** — 유저 퀴즈 상세/편집(`UserQuizInfoUI`). 삭제 버튼이 `interaction.user.id === PRIVATE_CONFIG.ADMIN_ID` 여부로 `quiz_delete_confirm_comp`(일반, 2버튼) vs `quiz_delete_confirm_admin_comp`(관리자, 3버튼: 취소/삭제/삭제+영구밴) 분기. `onReceivedWebSessionSignal`/`reapplyFromWebPayload`(퀴즈 선택 웹 연동 Phase 2, 2026-08-08 신설) — `dev-quiz-info-ui.js`와 동일 패턴(확정 후 웹에서 재선택 시 같은 인스턴스를 갱신, 새 인스턴스 반환 안 함)이지만 DB 조회가 필요해 비동기(fire-and-forget 후 `refreshUI()`+`fillInfoAsDevQuizInfo()`를 직접 호출). 마지막 반영은 **`update()`가 아니라 `sendDelayedUI(this, true)`(강제 재전송, 2026-08-15 수정)** — 재선택한 퀴즈의 썸네일이 이전 퀴즈와 다를 수 있는데, Discord embed edit로는 새 이미지 URL이 간헐적으로 안 불러와지는 버그가 있어서(`user-question-info-ui.js`의 "24.05.07 embed 이미지 버그"와 동일 원인) `update()`를 쓰면 이미지가 안 바뀐 채로 보일 수 있었음. `quiz_toggle_public` 핸들러의 "태그 1개 이상 있어야 공개 전환 가능" 조건은 2026-08-11부터 `quiz_editor_validation.canGoPublic(tags_value)` 호출로 바뀜(퀴즈 만들기 웹 연동 Phase 2, 아래 `user-question-info-ui.js` 항목 참고).
- **`quiz_editor_validation.ts`**(`quizbot/managers/`, 신규 2026-08-11) — 퀴즈 만들기 웹 연동
  (`docs/plans/WEB_QUIZ_CREATION_PLAN.md`) Phase 2. `user-question-info-ui.js`/`user-quiz-info.ui.js`의
  인터랙션-비의존 검증/파싱 순수 함수 7개(`parseAudioRangePoints`/`redefineRepeatCount`/
  `parseUseAnswerTimer`/`isValidAudioUrl`/`isValidImageUrl`/`isDiscordCdnLink`/`canGoPublic`)를 모아둔
  공유 모듈 — `multiplayer_mmr.js`와 동일 관례. 디스코드 UI가 지금 이 함수들을 호출하고, Phase 3~4의
  REST 핸들러(`web_quiz_editor_routes.ts`)도 같은 함수를 재사용할 예정.
- **`multiplayer-quiz-select-ui.js`** — 멀티플레이 로비 생성/참가 진입점. `checkMultiplayerBanMessage(interaction)`(2026-08-12, UI 개선 2라운드 B-5 — 원래 이름 `checkMultiplayerBan(list)`)이 `ban_manager.isBanned(...)`로 위임하되, 유저ID/길드ID를 따로 검사해서 어느 쪽이 밴됐는지 구분하는 메시지(+문의처 안내)를 반환한다 — 예전엔 `[guild.id, user.id]`를 한 배열로 묶어 검사해서 "당신 또는 이 서버가..."로 뭉뚱그려 안내했음. 같은 증상이 있는 `web-handoff-ui.ts`의 `buildMultiplayerUI`(웹 경로)는 아직 안 고침.
- **`admin-panel-ui.js`**/**`admin-ban-list-ui.js`** — `/quizmgr` 관리자 패널(밴 목록 관리/신고처리/퀴즈 관리/공지 관리/점검 모드/실시간 공지 수정/시즌 관리/로비 관리, 2번째 줄 4버튼 — 점검 모드+실시간 공지 수정은 2026-08-15 추가, 시즌 관리는 같은 날 후속 추가, 로비 관리는 2026-08-29 추가). 다른 유저는 절대 접근 불가하도록 다층 방어(루트 `CLAUDE.md`의 "관리자 전용 기능" 참고). `AdminPanelUI` 자체가 "실시간 공지 수정" 버튼→모달 흐름을 직접 처리함(별도 화면 전환 없음, `managers/notice_manager.ts`의 `readCurrentNotice`/`writeCurrentNotice` 위임).
- **`admin-lobby-list-ui.ts`**/**`admin-lobby-detail-ui.ts`**(quizmgr 멀티플레이 로비 관리, 2026-08-29 신설) —
  `AdminPanelUI`의 "🎮 로비 관리" 버튼에서 진입. `AdminNoticeListUI`/`AdminNoticeDetailUI`와 동일한
  "목록→상세→확인" 패턴 — `AdminLobbyListUI`는 `ipc_manager.sendMultiplayerSignal({signal_type:
  CLIENT_SIGNAL.REQUEST_LOBBY_LIST, ...})`로 마스터 프로세스의 로비 레지스트리를 조회(신규 신호 없이
  기존 신호 재사용, `managers/CLAUDE.md`의 "멀티플레이" 섹션 참고)해 LOBBY+INGAME 전체를 select
  메뉴로 보여준다. `AdminLobbyDetailUI`는 선택된 로비가 INGAME이면 관리 버튼 자체를 안 보여주고(진행
  중 로비는 삭제 불가), LOBBY면 "🗑 로비 강제 삭제" → 확인 절차(`quiz_delete_confirm_admin_comp`와
  동일 패턴, 취소/삭제/삭제+영구밴 2개 ActionRow) → 확정 시 `CLIENT_SIGNAL.ADMIN_FORCE_DELETE_LOBBY`
  신호로 마스터에 강제 삭제 요청(성공하면 참가 길드 전체에 기존 정상 종료와 동일한
  `SERVER_SIGNAL.EXPIRED_SESSION`이 브로드캐스트됨) → "삭제+영구밴"이면 성공 응답 이후 클러스터
  로컬에서 `ban_manager.banId(host_guild_id, actor)`를 바로 호출(IPC 불필요,
  `user-quiz-info.ui.ts`의 "퀴즈 삭제+제작자 영구밴" 버튼과 동일 패턴) — 밴 대상은 방장 길드만.
- **`admin-notice-list-ui.ts`**/**`admin-notice-detail-ui.ts`**(quizmgr 공지 관리, 2026-08-15 신설) —
  `AdminPanelUI`의 "공지 관리" 버튼(`admin_panel_notice_manage`)에서 진입. `AdminNoticeListUI`는
  `AdminBanListUI`와 동일 패턴(select 메뉴, 최대 25개)으로 `notice_manager.loadNoticeList` 목록을
  보여주고 "새 공지 작성" 버튼(모달, 제목+본문)이 있음. 항목 선택 시 `AdminNoticeDetailUI`로 전환 -
  본문 미리보기 + 수정(모달, 기존 값 프리필)/삭제(밴 해제와 동일한 확인 절차 - `admin_ban_unban_confirm_comp`
  패턴 재사용) 버튼. 삭제 확정 시 `this.goToBack()`으로 목록 화면으로 돌아가고,
  `AdminNoticeListUI.onAwaked()`가 뒤로가기 시점마다 목록을 다시 불러와 최신 상태를 반영한다(작성/수정/
  삭제 어느 경로로 돌아와도 동일하게 동작). 파일 I/O 자체는 `managers/notice_manager.ts`의
  `writeNoticeFile`/`updateNoticeFile`/`deleteNoticeFile`에 위임(그 파일 CLAUDE.md 참고).
- **`admin-maintenance-ui.ts`**(quizmgr 점검 모드 관리, 2026-08-15 신설) — `AdminPanelUI`의 "🔧 점검
  모드" 버튼에서 진입. 켜짐/꺼짐 상태를 embed로 보여주고(꺼짐: 초록, 켜짐: 빨강 + 현재 안내 문구
  표시), 버튼 구성이 상태에 따라 갈림 — 꺼짐이면 "켜기"(모달로 안내 문구 입력) 1개, 켜짐이면 "문구
  수정"(모달, 기존 값 프리필)/"끄기"(밴 해제와 동일한 확인 절차) 2개. "켜기"와 "문구 수정"은 같은
  모달(`modal_maintenance_notice`)과 같은 제출 핸들러를 공유함(둘 다 파일에 덮어쓰기만 하면 되는 동일
  동작이라). 파일 I/O는 `managers/maintenance_mode_manager.ts`에 위임 — **주의**: 점검 모드가 켜지면
  `bot.js` 전역 핸들러가 관리자 외 전 유저의 인터랙션을 차단하지만, 관리자 본인은 그 체크를 그대로
  통과하므로 점검 모드 중에도 `/quizmgr`로 들어와 끌 수 있음(자기 자신을 잠글 걱정 없음).
- **`admin-season-ui.ts`**(quizmgr 시즌 관리, 2026-08-15 신설, `docs/plans/SCOREBOARD_SEASON_PLAN.md`) —
  `AdminPanelUI`의 "🏆 시즌 관리" 버튼에서 진입. 현재 시즌 이름 + 지금까지 종료된 시즌 개수를 embed로
  보여주고, "🏆 시즌 종료 및 새 시즌 시작" 버튼 1개 → 밴 해제와 동일한 2버튼 확인 절차(`admin_season_end_confirm_comp`)
  → 확정 시 별도 reply 없이 바로 `interaction.showModal(modal_new_season_name)`으로 새 시즌 이름을 입력받음
  → 모달 제출 시 `scoreboard_season_manager.endSeasonAndStartNew`(위 `managers/CLAUDE.md` 참고) 호출,
  성공하면 화면을 새로고침하고 에페메럴로 결과 안내(실패 시 — DDL 미실행 등 — 에페메럴 실패 안내만 하고
  화면은 그대로 둠).

## 그 외 화면 (탐색 결과 요약)

**퀴즈 선택/시작 흐름**
- **`select-ui-mode-ui.js`** (신규, 2026-08-08) — `/퀴즈` 최초 진입 화면(`ui-system-core.js`의 `createMainUIHolder`가 `MainUI` 대신 이걸 먼저 띄움). "디스코드 UI"/"웹 UI" 두 트랙 분기: `'1'`(디스코드) → `MainUI`(기존 방식 100% 그대로), `'2'`(웹) → `MainUI`/`SelectQuizTypeUI`를 건너뛰고 곧장 `WebHandoffUI('user', interaction)`(원격 컨트롤). **초기 탭은 2026-08-15까지 `'dev'`(공식 퀴즈)였다가 사용자 피드백으로 `'user'`(유저 퀴즈)로 변경됨** — `'user'`는 세션 생성 시 초기 탭일 뿐 세션이 그 모드에 고정되는 건 아님 — 웹 프론트엔드에서 유저 퀴즈/랜덤 퀴즈 탭으로 전환해도 정상 동작함(Phase 1/2/3 전부 완료, 아래 `web-handoff-ui.js` 참고). 버튼 컴포넌트(`components/base_components.ts`의 `select_ui_mode_btn_component`, `quiz-edit-select-ui-mode-ui.ts`와 공유)는 2026-08-12부터 라벨이 `'디스코드 UI'`/`'웹 UI'` 텍스트(이전엔 숫자 `'1'`/`'2'`뿐) — `customId`는 그대로 `'1'`/`'2'`. 상세는 `docs/plans/WEB_INTEGRATION_PLAN.md`. **2026-08-13** — 실시간 공지(`resources/current_notice.txt`)를 `bot.js`가 `/퀴즈` 처리 중 별도 `channel.send()`로 보내던 걸 이 화면의 embed 필드(`buildNoticeFields()`)로 옮김 — 트랙 선택 전에만 뜨는 화면이라 웹 UI 선택 후에는 자동으로 안 보임. 설명과 공지 사이 시각적 구분을 위해 빈 이름(zero-width space) + 가로줄 문자 필드를 구분선으로 추가. "권한 가져오기"(`ui_force_take`, 아래 `createMainUIHolder` 항목)로 다른 유저에게서 화면을 넘겨받은 경우도 이 화면이 뜬다 — 별도 인자 없이 일반 `/퀴즈` 진입과 완전히 동일하게 동작(웹 UI를 고르면 새 세션을 그냥 새로 발급받음, 재사용 안 함 — 애초에 이전 홀더가 이미 `free()`돼서 재사용할 대상이 없기도 하고, 재사용해봐야 절약되는 비용이 미미해서 단순한 쪽을 선택함).
- **`select-quiz-type-ui.js`** — "개발자 퀴즈/유저 제작 퀴즈/오마카세 퀴즈" 선택 (main-ui의 '3'번에서 진입). **디스코드 트랙 전용 화면이라 웹 연동과 무관** — `'1'`(공식)은 다시 `DevQuizSelectUI`로 원복됨(2026-08-08, 투트랙 분리 — 예전엔 여기서 `WebHandoffUI`로 가로챘었지만 그 진입점이 `select-ui-mode-ui.js`로 위로 옮겨감). Phase 2(유저 퀴즈 웹 연동) 작업도 이 파일을 건드리지 않음 — 웹 쪽 유저 퀴즈 지원은 전부 `select-ui-mode-ui.js`→`WebHandoffUI` 경로에서 처리됨.
- **`quiz-edit-select-ui-mode-ui.ts`**/**`quiz-edit-web-handoff-ui.ts`** (신규, 2026-08-11) — 퀴즈 만들기 웹 연동(`docs/plans/WEB_QUIZ_CREATION_PLAN.md`) Phase 1. `select-ui-mode-ui.ts`/`web-handoff-ui.ts`와 같은 투트랙 진입 패턴이되 `/퀴즈만들기`(`createQuizToolUIHolder`)가 항상 DM(PRIVATE, holder_id=user_id)에서만 호출되므로 하이재킹 방어(force_take/already_locked)가 통째로 빠짐 — DM은 봇-유저 1:1이라 애초에 다른 유저가 이 화면을 볼 수 없다. `QuizEditWebHandoffUI`는 마스터의 `web_session_manager`에 `create_owner_session`(owner_id 단위 세션, guild 개념 없음) IPC 요청을 보내고, 편집 링크 Link 버튼 1개로 잠금 화면을 구성한다. Phase 1은 아직 실제 편집 화면이 없어 `onReceivedWebSessionSignal`이 `'expired'`만 처리(`applied`는 문제/퀴즈 CRUD가 붙는 Phase 3~4에서 추가 예정).
- **`web-handoff-ui.js`** (신규, 2026-08-08) — 퀴즈 선택 웹 연동(`docs/plans/WEB_INTEGRATION_PLAN.md`) 잠금 화면. `select-ui-mode-ui.js`의 "웹 UI" 버튼에서 진입(이전엔 `select-quiz-type-ui.js`의 '공식 퀴즈' 버튼에서 진입했었음 — 뒤로가기/GC만료 복귀 대상 화면도 그에 맞춰 `SelectQuizTypeUI`→`SelectUIModeUI`로 바뀜). 마스터의 `web_session_manager`와 `WEB_SESSION_REQUEST`/`WEB_SESSION_SIGNAL` IPC로만 통신, 실제 세션 상태는 안 갖고 있음. `onReceivedWebSessionSignal`이 `updated`(선택 요약 갱신)/`applied`(다음 UI로 전환, `handleApplied`)/`expired`(`goToBack()`)를 처리 — `applied`에서 DB 조회가 필요한 모드(유저 퀴즈)는 `UIHolder.on()`이 동기 계약이라 `this.holder.appendNewUI()`/`updateUI()`를 직접 호출하는 우회가 필요함(dev/omakase 모드는 동기 데이터라 이 우회가 없음). **`handleApplied`는 `this.mode`(세션 생성 시 고정값)가 아니라 `payload.mode`로 분기한다**(2026-08-08 Phase 2 - 프론트엔드가 select/confirm 요청마다 자기 탭의 mode를 실어보내므로, 한 세션에서 dev/user/omakase 탭을 자유롭게 오갈 수 있음). `buildUserQuizInfoUI(payload)`(Phase 2, 비동기) — `loadUserQuizInfoById`로 조회 후 `UserQuizInfoUI` 생성, `quiz_info['selected_question_count']`를 `onReady()` 호출 전에 미리 채워서 기본값(전체 문제 수)으로 덮어써지지 않게 함. `buildOmakaseQuizInfoUI(payload)`(Phase 3, 2026-08-08 신규, 동기) — DB 조회가 필요 없는 작은 데이터라 dev와 동일하게 즉시 반환, `OmakaseQuizRoomUI.buildOmakaseQuizInfoFromWebPayload`로 조립(2026-08-18부터 `QuizInfoUI.BASKET_CACHE` 갱신은 안 함 — 퀴즈함 관리+프리셋 UI 신설로 오마카세 쪽 BASKET_CACHE 소비처가 없어짐, 아래 `basket-manage-flow.ts` 항목 참고). `buildMultiplayerUI(payload)`(Phase 4, 2026-08-10 신규, 비동기) — `MultiplayerQuizSelectUI.createLobby`/`tryJoinLobby`와 동일한 밴 체크(`ban_manager.isBanned`)/음성채널 체크(`quiz_system.checkReadyForStartQuiz`)를 그 클래스를 거치지 않고 직접 재현한다(음성채널 체크는 진짜 `GuildMember.voice`가 필요해 `guild.members.fetch`로 실제 멤버를 조회 - `{member:{id}}` 같은 최소 어댑터로는 불가능). 통과하면 `{guild, member, channel, web_mode:true}` 어댑터로 `MultiplayerQuizLobbyUI`를 직접 생성(`action:'create'`는 `MultiplayerQuizLobbyUI.buildMultiplayerQuizInfoFromWebPayload`로 quiz_info 조립, `action:'join'`은 `payload.session_id`로 참가) - `MultiplayerQuizLobbyUI` require는 함수 안으로 미룸(아래 항목 참고, dev-quiz-info-ui.js 순환 require 수정과 동일한 이유). **주의**: "선택 완료(applied)"는 토큰을 파기하지 않음(2026-08-08 재설계) — 화면은 `DevQuizInfoUI`/`UserQuizInfoUI`/`OmakaseQuizRoomUI`/`MultiplayerQuizLobbyUI`로 넘어가지만 웹 세션은 계속 살아있고, 그 뒤 재선택은 각 정보 화면이 처리함(아래). **밴/음성채널 체크 실패 시 `goToBack()`을 부르지 않음**(2026-08-10 실사용 피드백 수정 - 원래 `goToBack()`을 불렀는데, `WebHandoffUI` 자신이 `prev_ui_stack`의 이전 화면(`SelectUIModeUI`)으로 교체돼버려서 이후 웹에서 재시도해도 신호를 받을 화면이 없어 완전히 먹통이 됐었음). 대신 `this.last_error`를 채우고 `refreshLockedEmbed()`로 잠금 화면에 실패 사유를 얹어 보여주면서 `WebHandoffUI`를 유지 - 재시도가 정상 동작함. 성공/실패 여부는 `ipc_manager.sendWebSessionRequest({action:'report_multiplayer_result', ...})`로 마스터의 `web_session_manager`에 보고되고, 프론트엔드는 `/api/multiplayer-result`를 잠깐 폴링해서 실제 결과를 읽어온다(웹에도 진짜 에러 메시지가 뜨게 됨) - 다만 이건 밴/음성채널 체크(사전 검증) 결과만 보고하고, 그 이후 `MultiplayerQuizLobbyUI` 내부의 딥 IPC(실제 로비 생성/참가) 실패는 낙관적으로 성공 취급(드문 케이스, 그 시점엔 이미 `MultiplayerQuizLobbyUI`가 `prev_ui_stack`에 `WebHandoffUI`를 쌓아둔 채라 실패해도 자체 `goToBack()`으로 안전하게 복귀됨). 로비 생성 후에도 웹에서 계속 설정을 바꿔 재확정할 수 있다(2026-08-10 피드백) - 프론트엔드(`MultiplayerTab.jsx`)가 최초 생성 후엔 "대기실 목록으로"를 숨기고 같은 화면에서 계속 `confirmMultiplayer('create', ...)`를 재호출, 이 신호는 이미 `MultiplayerQuizLobbyUI`가 current UI라 `WebHandoffUI.buildMultiplayerUI`를 다시 거치지 않고 `MultiplayerQuizLobbyUI.onReceivedWebSessionSignal`이 직접 처리함(밴/음성채널 재검증 없음, 기존 Omakase와 동일 패턴). **여전히 남은 한계**: 생성 폼은 설정을 만지는 동안 실시간 미리보기(select 브로드캐스트)가 없어 확정을 눌러야만 디스코드에 반영됨. **`refreshLockedEmbed`(2026-08-12, 웹 API 보안 점검 심화 조사)**: `current_selection.title`(`POST /api/session/select`의 진행 중 미리보기 payload, 서버에서 길이 검증을 안 함)을 그대로 이 길드의 공유 embed에 꽂고 있었음 — UI를 거치지 않고 직접 API를 호출하면 임의 길이의 문자열을 다른 길드원에게 보이는 화면에 넣을 수 있었던 지점이라 표시 직전에 60자로 자르도록 수정.
- **`dev-quiz-select-ui.js`** — 공식 퀴즈 폴더 트리 브라우저. `static resource_path`/`quiz_contents_sorted_by_name`이 **클래스 정의 시점에 한 번만** 로드됨 — 실행 중 파일 추가해도 재시작 전까진 안 보임. `static generateDevQuizInfo(content)`/`static findContentByPath(content_path)`/`static buildDevQuizInfoFromWebPayload(payload)`(웹 연동용, 앞의 둘 + 문제 수 클램프를 묶은 헬퍼 — `web-handoff-ui.js`와 `dev-quiz-info-ui.js`가 공유해서 씀)는 인스턴스 없이도 호출 가능하게 static.
- **`dev-quiz-info-ui.js`** — `QuizInfoUI`를 감싸기만 하는 얇은 래퍼(`DevQuizInfoUI`). `onReceivedWebSessionSignal`(2026-08-08 신규) — 확정 후에도 웹에서 재선택하면(`'applied'` 신호) `buildDevQuizInfoFromWebPayload`로 자기 `quiz_info`/`embed`를 갱신하고 `this`를 반환(새 인스턴스 반환 시 `prev_ui_stack`이 계속 쌓여 "뒤로가기"가 이전 웹 선택들을 되짚는 부작용이 있어서 그 대신 같은 인스턴스를 재사용).
- **`omakase-quiz-room-ui.ts`** — 로컬(비-멀티) 오마카세 퀴즈 설정방. 태그/장바구니 모드 지원(`QuizInfoUI`의 no-op을 오버라이드). `static applyWebPayloadToQuizInfo(quiz_info, payload)`/`static buildOmakaseQuizInfoFromWebPayload(payload, adapter_interaction)`(퀴즈 선택 웹 연동 Phase 3, 2026-08-08 신규 — dev-quiz-select-ui.js의 웹 연동 static 헬퍼와 동일 패턴, `WebHandoffUI`와 자기 자신(재선택)이 공유) + `onReceivedWebSessionSignal`(dev/user와 동일 패턴, DB 조회가 없어 동기로 같은 인스턴스 갱신). **퀴즈함 관리+프리셋(2026-08-18 신설, `docs/plans/QUIZ_BASKET_PRESET_UI_PLAN.md`)**: 바구니 모드일 때 메인 화면엔 이제 "퀴즈함에 퀴즈 더 담기"/"🧺 퀴즈함 보기"(`omakase_basket_manage_open_comp`) 2버튼만 남고, 퀴즈함 표시/제거/프리셋 저장·불러오기·관리(이름변경/항목제거/전체삭제)는 전부 `basket-manage-flow.ts`(아래 항목)의 독립 ephemeral 화면으로 이동 — 예전엔 이 화면에 바구니 select 메뉴+"최근 퀴즈함으로 덮어쓰기"(`BASKET_CACHE` 기반) 버튼까지 있어서 컴포넌트 행이 4/5까지 찼었음, 분리 후 3/5로 줄어듦. `onInteractionCreate`가 `isTagSelectedEvent` 다음으로 `basket_manage_flow.isBasketManageEvent`/`isBasketManageModalEvent`를 체크해서 해당 customId면 화면 전환 없이 위임한다(`super.onInteractionCreate`로 안 내려감). 퀴즈함 담기 상한도 이 화면 한정으로 25→50→100 확장(`new UserQuizSelectUI(basket_items, OMAKASE_MAX_BASKET_SIZE)`, 2026-08-19 재확장으로 100까지 — `UserQuizSelectUI`의 `max_basket_size` 생성자 2번째 인자 기본값(25)은 바구니 모드가 아닌 호출부용으로 그대로 둠). **2026-08-19 멀티플레이 로비(`multiplayer-quiz-lobby-ui.js`)에도 동일 기능이 이식되며 `BASKET_CACHE`는 완전히 삭제되고 담기 상한도 동일하게 100으로 맞춰짐** — 아래 `multiplayer-quiz-lobby-ui.js`/`quiz-info-ui.ts`/`basket-manage-flow.ts` 항목 참고.
- **`multiplayer-quiz-lobby-ui.js`** — 멀티플레이 로비(IPC 기반, 가장 복잡). `CLIENT_SIGNAL`/`SERVER_SIGNAL` 송수신. 60초마다 메시지 신선도 체크(`checkNeedToRefresh`, 10분 넘으면 강제 재전송 — Discord 인터랙션 토큰 만료 대응). `requestKick`에 `selected_value > this.participant_guilds_info`(문자열-배열 비교, 항상 false인 죽은 검증)로 보이는 버그 있음. **멀티플레이 웹 연동(Phase 4, 2026-08-10)**: `WebHandoffUI.buildMultiplayerUI`가 실제 interaction 없이 `{guild, member, channel, web_mode:true}` 어댑터로 이 클래스를 생성한다 - `this.is_web_origin`이 true면 `connectToMultiplayerSession`이 모달 필드를 읽는 `applyQuizSettings(interaction)`를 건너뛰고(quiz_info는 이미 payload로 완성돼 있음), `requestToJoinLobby`/`requestToCreateLobby`는 존재하지 않는 `interaction.reply()` 대신 로그만 남기고 실패 시 `goToBack()`한다. `static applyWebPayloadToQuizInfo`/`buildMultiplayerQuizInfoFromWebPayload`는 quiz_info shape이 랜덤 퀴즈와 동일해서 `OmakaseQuizRoomUI.applyWebPayloadToQuizInfo`를 그대로 재사용하고 방 제목만 추가로 덮어씀 - 단, 오마카세엔 없는 `min_quiz_size`(멀티는 20) 하한은 공용 헬퍼가 모르기 때문에, 호출 후 `selected_question_count`를 `min_quiz_size` 이상으로 다시 한번 강제하는 코드가 별도로 있음(2026-08-10 수정 - 안 하면 웹에서 20개 미만도 확정 가능했음, 디스코드 모달 경로인 `quiz-info-ui.ts`의 `applySelectedQuestionCount`에만 있던 하한 체크가 웹 경로엔 원래 없었음). `onReceivedWebSessionSignal`(호스트 전용, `OmakaseQuizRoomUI`와 동일 패턴) — 로비 생성 후에도 웹에서 재확정하면 `this.sendEditLobbySignal()`로 참가 길드 전체에 브로드캐스트. **퀴즈함 관리+프리셋 이식(2026-08-19, `basket-manage-flow.ts` 항목 참고)**: 호스트 길드 화면은 기존 "바구니 select+최근 퀴즈함으로 덮어쓰기"(2행) 대신 `multiplayer_basket_manage_open_comp`(1행, "퀴즈함에 퀴즈 더 담기"+"🧺 퀴즈함 보기")로 교체(host 쪽은 이미 컴포넌트 5행이 꽉 차 있어서 이 압축이 필수였음), 참가 길드 화면도 자체 읽기전용 뷰어 대신 `multiplayer_basket_view_comp`(1행, "🧺 퀴즈함 보기"만)로 교체. `onInteractionCreate`가 `isTagSelectedEvent`보다 먼저 `basket_manage_flow.isBasketManageEvent`/`isBasketManageModalEvent`를 체크해서 위임한다. `handleLoadBasketItems`(구 `BASKET_CACHE` 소비처)는 완전히 삭제됨(원문은 `docs/archive/DEPRECATED_CODE_REMOVED.md`). **권한 모델이 오마카세보다 한 단계 더 있음**: `basket-manage-flow.ts`의 `is_host` 판별이 `room_owner` 일치뿐 아니라 `room_ui.readonly !== true`도 같이 확인 — 방장 본인이 다른 서버(참가 길드)에서도 그 계정으로 참가하면 `user.id`는 room_owner와 같아지지만 그 길드는 `readonly=true`라 여전히 조작 불가(호스트 길드에서만 조작 가능, 상세 근거는 `basket-manage-flow.ts` 파일 상단 주석). 항목 제거/불러오기로 라이브 퀴즈함이 바뀌면 오마카세처럼 로컬 `refreshUI()`/`update()`가 아니라 `sendEditLobbySignal()`로 참가 길드 전체에 IPC 브로드캐스트(`basket-manage-flow.ts`의 `syncRoomUI` 참고, duck typing으로 오마카세/멀티 분기). 담기 상한(`UserQuizSelectUI`의 `max_basket_size`)은 처음엔 이식 범위 밖이라 25 그대로 뒀다가, 같은 날 후속으로 오마카세와 동일하게 100까지 확장됨(`MULTIPLAYER_MAX_BASKET_SIZE`, `handleRequestUseBasketMode`의 두 `new UserQuizSelectUI(...)` 호출 모두 적용) — 100개면 `basket-manage-flow.ts`의 메인 화면이 select 4행+버튼 1행으로 Discord 5행 한도를 정확히 다 씀(여유 없음, 상세는 `basket-manage-flow.ts` 상단 주석).
- **`quiz-info-ui.ts`** — `DevQuizInfoUI`/`OmakaseQuizRoomUI`/`MultiplayerQuizLobbyUI` 공통 베이스(`QuizInfoUI`). 시작/설정/태그선택/바구니 버튼 핸들러 맵이 여기 있음 — 새 퀴즈 설정 화면 만들 때 여기부터 볼 것. `scoreboard` 버튼 핸들러는 **TODO 빈 껍데기**(실제 스코어보드는 `multiplayer-quiz-select-ui.js`에서 별도 진입). `handleStartQuiz`(2026-08-08)는 퀴즈 시작 시 그 길드의 웹 연동 세션(있다면)을 `WEB_SESSION_REQUEST{action:'release'}`로 조용히 파기 — fire-and-forget, 세션 없는 길드에도 안전. **`BASKET_CACHE`(길드별 장바구니 캐시 static)와 그걸 쓰던 `basket_select_component`/`setupBasketSelectMenu`/`handleBasketSelected`/`handleLoadBasketItems`는 2026-08-19 멀티플레이 로비까지 퀴즈함 관리+프리셋 UI가 이식되며 마지막 소비처가 사라져 전부 삭제됨**(오마카세는 2026-08-18에 이미 뗐고, 그땐 멀티플레이가 유일한 남은 소비처라 남겨뒀었음 — 원문은 `docs/archive/DEPRECATED_CODE_REMOVED.md`). 새 구현은 `basket-manage-flow.ts`(아래 항목) 참고. `getTagInfoText()`(오마카세/멀티 둘 다 자기 `refreshUI()`에서 호출, 영구 메시지의 "📗 유저 퀴즈 설정" 섹션 조립)의 바구니 모드 분기는 2026-08-19부터 담긴 개수도 같이 보여줌(`🔸 \`퀴즈함 모드 사용 중\` (N개 담김)`) — 가시성 피드백 반영, `this.quiz_info['basket_items']`를 직접 세므로 room_ui 종류 구분 불필요.
- **`basket-manage-flow.ts`**(2026-08-18 신설, `docs/plans/QUIZ_BASKET_PRESET_UI_PLAN.md`, 같은 날 후속으로 방장 권한 모델 + 프리셋 관리(이름변경/항목제거) 추가, 2026-08-19 멀티플레이 로비까지 이식) — 퀴즈함 표시/제거 + 프리셋 저장·불러오기·관리를 담당하는 독립 ephemeral 화면. **`OmakaseQuizRoomUI`/`MultiplayerQuizLobbyUI` 공용** — `room_ui` 인자 자리에 둘 중 아무거나 넘겨받아 동작(구분은 `room_ui.readonly` 존재 여부/`room_ui.sendEditLobbySignal` 존재 여부로 duck typing, 아래 참고). **`QuizbotUI`/`UIHolder`를 상속하지 않는 유일한 화면류** — `admin-panel-ui.ts`가 `report_manual_processing.sendReportLog`를 호출만 하고 자체적으로 응답을 처리하게 두는 기존 패턴과 같은 계열로, 두 room_ui 클래스의 `onInteractionCreate`가 각자 `isBasketManageEvent`(버튼/셀렉트, `basket_manage_` 접두사)/`isBasketManageModalEvent`(모달 제출, `modal_basket_preset_` 접두사 — 별도 체크 필요, 안 하면 모달 제출이 조용히 무시됨) 둘 다 확인해서 `handleBasketManageEvent`로 위임만 하고 화면 전환은 전혀 안 함(`handleBasketManageEvent`는 내부 핸들러가 async여도 항상 `undefined`를 동기 반환 — 2026-08-15 `admin-season-ui.ts`에서 겪은 "Promise를 그대로 반환해 크래시" 버그와 동일 함정을 의도적으로 피함). `interaction.reply()`로 최초 ephemeral 응답을 만들고 이후 컴포넌트는 `interaction.update()`(discord.js가 제공하는, 컴포넌트가 속한 메시지 자체를 편집하는 API — 이 시점 기준 `quiz_ui/` 전체에서 이 파일이 유일하게 씀)로 자기 자신만 갱신. **메인 화면(`OmakaseQuizRoomUI`/`MultiplayerQuizLobbyUI`)과 완전히 독립된 메시지라서, 퀴즈함이 바뀔 때마다 `syncRoomUI(room_ui)`를 호출해 메인 화면 "퀴즈함 N개" 문구를 명시적으로 동기화함** — 안 하면 두 메시지가 서로 다른 상태로 벌어짐(설계 중 발견한 핵심 함정). `syncRoomUI`는 `room_ui.sendEditLobbySignal`이 있으면(`MultiplayerQuizLobbyUI`만 가짐) 로컬 갱신 대신 그쪽으로 IPC(`CLIENT_SIGNAL.EDIT_LOBBY`) 브로드캐스트를 태워 참가 길드 전체에 동기화하고, 없으면(오마카세) 기존처럼 `room_ui.refreshUI()`+`room_ui.update()`. **권한 모델(2026-08-19 실사용 피드백 + 멀티플레이 이식으로 세분화)**: 라이브 퀴즈함(공유 상태)을 실제로 "바꾸는" 액션(항목 제거/불러오기 — 불러오기는 통째로 덮어씀)만 방장 전용. **조회와 "현재 퀴즈함을 프리셋으로 저장"은 방장 여부 무관하게 누구나 가능** — 저장은 라이브 상태를 읽기만 할 뿐 건드리지 않아서다. 그래서 항목 select는 방장이 아니어도 `.setDisabled()` 하지 않음(비활성화하면 드롭다운을 열어볼 수조차 없어 "조회"마저 막혀버리는 게 최초 설계의 버그였음 — 선택해서 제출해도 제거 대신 "🔒 퀴즈함에서 제거하는 건 방장만 할 수 있어요" 안내만 뜨고 되돌아감). **"방장만 ~할 수 있어요" 류 상시 안내는 2026-08-19 피드백으로 제거됨** — 메인 화면 설명/select placeholder에는 더 이상 이 문구가 안 뜨고(화면이 항상 그 설명을 달고 있을 필요는 없다는 판단), 실제로 제거를 시도했을 때만 위 반응형 안내가 뜬다. 방장 판별은 `interaction.user.id === room_ui.quiz_info['room_owner'] && room_ui.readonly !== true` 두 조건 AND — 오마카세는 길드가 하나뿐이라(`readonly`가 항상 `false`) `room_owner` 비교만으로 충분하지만, 멀티플레이는 로비를 만든 "호스트 길드"(`readonly=false`)와 나중에 참가만 한 "참가 길드"(각자 별도 인스턴스, `readonly=true`)가 따로 있어서 `room_owner`만 보면 방장 본인이 다른 서버(참가 길드)에서 같은 계정으로 참가했을 때 그 길드에서도 조작 권한이 생겨버리는 구멍이 생김 — `readonly` 체크가 그걸 막는다. 메인 화면 버튼은 방장이면 저장/불러오기/관리 3개, 방장이 아니면 불러오기만 빠진 저장/관리 2개. 방장이 항목을 지워도 이미 열려있는 다른 사람의 조회 화면엔 실시간 반영 안 함(설계 확정 — ephemeral 특성상 자연스러움). **화면 상태 4개**(전부 무상태 — 인스턴스에 안 들고 customId에 `preset_id`/페이지 번호를 직접 인코딩해 처리, 예: `basket_manage_manage_item_select:${preset_id}:${page}`): `main`(퀴즈함 목록+저장/불러오기/관리) → `preset_load_list`(방장 전용, 선택 즉시 라이브 퀴즈함에 로드) / `preset_manage_list`(내 프리셋 목록, "웹 UI에서 편집하면 더 편해요" 안내 포함) → `preset_manage_detail`(선택한 프리셋의 항목 제거 + 이름변경(`modal_basket_preset_rename:${preset_id}:${page}`, 모달 prefill은 렌더링된 embed 텍스트 파싱이 아니라 DB 재조회로 얻음 — 문구 바뀌면 깨지는 취약한 방식을 피함)/전체삭제 — 삭제 확인 화면도 같은 이유로 DB 재조회해서 `"${preset_name}" 프리셋을 정말 삭제하시겠어요?`로 이름을 보여줌, 2026-08-19 피드백으로 추가됨). **프리셋 항목 제거 UI는 페이지네이션(2026-08-18 세 번째 후속, `scoreboard-ui.ts`의 TOP50 페이지네이션과 동일 패턴)** — 프리셋 자체는 웹에서 최대 100개까지 저장 가능(RANDOM_QUIZ_PRESET_ITEM_MAX_COUNT, 2026-08-19부터 라이브 퀴즈함 한도와 같은 값이지만 애초에 별개 상한)해서 처음엔 앞 50개만 보여주고 "웹에서 편집" 안내로 대응했다가, "이 페이지네이션 방식을 쓰면 100개도 디스코드에서 다 지원되지 않냐"는 사용자 피드백으로 select 1행+prev/next 버튼 1행(25개씩 페이지 이동)으로 교체 — 이제 상한 없이 전부 디스코드에서 편집 가능(행 구성은 select 1행+prev/next 1행+이름변경/삭제 1행+뒤로가기 1행=4행, 페이지가 1개뿐이면 prev/next 행 자체를 안 보여줌). 반면 라이브 퀴즈함(main 상태)은 페이지네이션 없이 flat 다중 행(25개씩)으로 그대로 두기로 함 — 100개 기준 select 4행+버튼 1행으로 정확히 5행이라 여유는 없지만, 지금 당장 문제될 건 없다는 판단(사용자 확인, 2026-08-19). 나중에 이 화면에 버튼 행을 추가해야 하는데 자리가 없으면 여기(프리셋 관리)와 동일한 페이지네이션으로 전환하면 됨. `handleManagePageChange`가 prev/next 버튼 공용 핸들러(delta -1/+1), 페이지 번호는 항상 `Math.min(Math.max(page,0), total_pages-1)`로 clamp해서 항목 제거로 페이지 수가 줄어도 안전. 프리셋 관련 DB 로직은 `db_random_quiz_preset.ts`(저장/불러오기/삭제는 웹과 100% 동일 함수 재사용, 이름변경(`updateRandomQuizPresetName`)/항목제거(`deleteRandomQuizPresetItem`)는 이번에 신설 — **웹 UI엔 아직 없는, 디스코드가 먼저 갖게 된 기능**)에 위임 — 전부 웹 세션과 무관한 순수 `user_id` 기반이라 웹/디스코드 어느 쪽에서 만든 프리셋이든 서로 호환. "불러오기"는 `db_quiz.ts`의 `selectQuizInfoByIds`(신규)로 저장된 quiz_id 목록을 제목과 함께 조회 — 삭제/비공개 전환된 항목은 결과에서 자동으로 빠지고 그 차이만큼 "N개 항목은 더 이상 사용할 수 없어 제외됐어요" 안내. 프리셋 목록 자체는 상태로 안 들고 매 인터랙션마다 다시 조회(유저당 최대 10개라 가벼움, 상태 동기화 버그보다 안전).
- **`user-quiz-select-ui.js`** — 유저 퀴즈 목록/검색/정렬 + 장바구니 담기 모드(생성자에 `basket_items` 넘기면 담기 모드로 전환). `onReady()`에서 전체 유저 퀴즈를 한 번에(`loadUserQuizListFromDB(undefined)`) 불러와 클라이언트 사이드에서 필터/정렬.
- **`user-question-info-ui.js`** — 유저 퀴즈의 문제 편집기. **파일 자체 주석: "건드릴 엄두가 안난다... 우선 돌아가면 장땡"** — 조심해서 다룰 것. 문제 최대 개수 제한은 `SYSTEM_CONFIG.MAX_QUESTIONS_PER_QUIZ`(50, 2026-08-11부터 상수화). 이미지 URL 변경 시 `update()` 대신 강제 재전송(`sendDelayedUI(this, true)`) — Discord embed edit이 새 이미지 URL을 바로 안 불러오는 문제 우회. `duplicateQuestion`(B-3', 2026-08-07)은 현재 문제를 복사해 바로 다음 위치에 삽입 — `interaction.explicit_replied = true`는 반드시 함수 맨 첫 줄에서 설정할 것(await 이후로 미루면 `bot.js` 전역 fallback이 먼저 `deferUpdate()`를 호출해 "Interaction has already been acknowledged" 에러가 남, 2026-08-08 수정). `sendAudioPreview`(B-2, 2026-08-07)는 미리듣기 버튼 핸들러 — `updatePrivateUI()`(카드 edit) 대신 `interaction.reply({files, flags: Ephemeral})`로 직접 응답(B-2-1 "새 메시지 금지" 방침의 명시적 예외, 상세는 `docs/plans/TS_MIGRATION_AND_CONVENIENCE_PLAN.md` B-2 섹션 참고). **2026-08-11 리팩터**: 오디오 구간 파싱(`parseAudioRangePoints`)/반복횟수 클램프(`redefineRepeatCount`)/타이머 문자열 매칭/URL 유효성 검사/디스코드 CDN 링크 감지 로직이 전부 `quiz_editor_validation.ts`(위 항목)로 추출되고 호출부만 남음 — 이 파일에 남은 건 `interaction`/모달/embed 조립 등 디스코드 전용 로직뿐(동작 변경 없는 순수 이관, `docs/plans/WEB_QUIZ_CREATION_PLAN.md` Phase 2). **Phase 4(문제 CRUD REST, 2026-08-11)에서 이 파일은 전혀 안 건드림** — `web/web_quiz_editor_routes.ts`의 `applyQuestionFields`가 이 파일의 `applyQuestionInfo`/`applyQuestionAdditionalInfo`/`applyQuestionAnsweringInfo`를 단일 함수로 합친 REST 버전으로, Phase 2에서 이미 추출된 `quiz_editor_validation.ts` 함수만 재사용해서 만들어졌기 때문.
- **`alert-quiz-start-ui.js`** — "퀴즈 시작합니다" 안내(정적, 인터랙션 없음). 모든 퀴즈 시작 경로의 종착점.
- **`scoreboard-ui.ts`** — 서버별/글로벌 랭킹. **시즌 아카이브 연동(2026-08-15 신설, `docs/plans/SCOREBOARD_SEASON_PLAN.md`)**: 기존엔 제목의 "[베타 시즌]" 부분이 `text_contents.json`의 고정 텍스트(`scoreboard.season_label`, 2026-08-12 이동)였고 실제 시즌 데이터는 없었음 — 이번에 실제 데이터 모델이 생기면서 그 텍스트는 제거되고 `scoreboard_season_manager.getCurrentSeasonName`으로 대체. `selectSeasonList()`로 종료된 시즌 목록을 불러와 StringSelectMenu(`scoreboard_season_select`, "현재 시즌"+최대 24개 과거 시즌)로 전환 가능하게 함 — `selected_season_id`가 `null`이면 현재 시즌(`selectGlobalScoreboard`/`selectTop50Scoreboard`), 아니면 아카이브(`selectArchivedGuildScoreboard`/`selectArchivedTop50Scoreboard`)를 로드. 시즌 목록이 비어있으면(DDL 미실행 또는 아직 종료된 시즌 없음) select 메뉴 자체를 안 보여주고 기존과 동일하게 뒤로가기만 있는 화면으로 폴백. **TOP50 페이지네이션(같은 날 후속, 사용자 요청)**: TOP10→TOP50으로 확장되며 embed 하나에 다 못 담아 자체 페이지네이션 도입 — `top_scoreboard_rows`(최대 50개, 한 번의 쿼리로 전부 로드)를 `top_page` 상태로 10개씩 슬라이스해서 보여주고, 넘칠 때만(50개 다 안 채워지면 버튼 자체가 없음) `scoreboard_top_prev`/`scoreboard_top_next` 버튼 노출(경계에서 `.setDisabled()`). 페이지 전환은 이미 로드된 배열을 슬라이스만 할 뿐 DB 재조회가 없어 동기 처리 — `QuizBotControlComponentUI`(번호 select 버튼 포함, 선택 가능한 목록 전용 프레임워크)는 이 화면(클릭 불가한 순수 표시용 랭킹)엔 안 맞아 안 쓰고 최소한의 자체 로직만 둠. 랭킹 표시는 1~3위만 메달 이모지, 4위부터는 `ICON_NUM_` 텍스트 이모지(원래 0~10까지만 존재해 TOP50엔 애초에 못 씀) 대신 그냥 평범한 `"N)"` 숫자로 통일.
- **`server-setting-ui.js`** — 서버 옵션 편집. `quiz_option.js`의 `OptionStorage`를 **클론해서** 편집하다가 "저장" 버튼을 눌러야 커밋됨 — 저장 안 하고 나가면 변경사항 사라짐(의도된 동작). 권한 체크(`ManageGuild` 등) 없음 — 서버의 아무나 편집 가능한 게 **의도된 기존 동작**(2026-08-12 사용자 확인, 조사로 발견됐지만 버그 아님). 웹 버전(`web-frontend/src/ServerSettingPanel.jsx`, `GET`/`PUT /api/server-option`)이 `quiz_option.js`의 같은 `OptionStorage` 메모리 캐시를 공유함 — 웹에서 바꾸면 이 화면도 즉시 같은 값을 보게 됨. **`handleResetOption`(2026-08-12, UI 개선 2라운드 B-4 [P2])** — "기본값으로 초기화" 버튼(`option_control_btn_component`에 신설) 핸들러, `quiz_option.getDefaultQuizOption()`으로 `option_data`를 되돌림 — `handleSaveOption`과 동일하게 [저장]을 눌러야 DB에 실제 반영됨(여기선 바로 안 씀).
- **`note-ui.js`** — 공지/패치노트 본문. 파일 읽기 로직은 `managers/notice_manager.ts`의 `readNoticeFile`로 추출됨.

## 나머지 화면 웹 포팅 (`docs/plans/WEB_UI_REMAINING_SCREENS_PLAN.md`, 2026-08-12)

퀴즈만들기 안내 페이지(`quiz-tool-guide-ui.js`)/공지사항(`note-select-ui.js`+`note-ui.js`)/서버 설정
(`server-setting-ui.js`)을 "퀴즈 선택 웹"(`select-ui-mode-ui.js`→`WebHandoffUI`, `web-frontend`
index.html/App.jsx)에도 노출. 새 진입점을 만들지 않고 App.jsx 헤더의 "☰ 더보기" 드롭다운(`utilityPanel`
state)에 3항목으로 묶임 — 퀴즈 선택 4-tab(`dev`/`user`/`omakase`/`multiplayer`)과 성격이 달라
`tabrail`에는 안 넣음. 신규 프론트엔드 컴포넌트 `GuidePanel.jsx`/`NoticesPanel.jsx`/
`ServerSettingPanel.jsx`(전부 `web-frontend/src/`), 백엔드는 `web_express_app.ts`에 인라인 라우트로
추가(`managers/CLAUDE.md`의 "퀴즈 선택 웹 연동" 항목 참고). 패치노트는 스코프 아웃(위 `note-select-ui.js`
항목 참고), 서버 설정은 권한 체크 없이 그대로 포팅(위 `server-setting-ui.js` 항목 참고).
**비주얼 개선(2026-08-15 피드백)**: `GuidePanel.jsx`/`NoticesPanel.jsx` 둘 다 `remark-breaks`를
`<Markdown remarkPlugins={[remarkBreaks]}>`로 연결(패키지는 이미 설치돼 있었으나 실제 연결이 안 돼
있어 원문의 단일 줄바꿈이 무시되던 문제 수정 - 원문은 디스코드 임베드와 그대로 공유하므로 손대지
않음). `GuidePanel.jsx`는 `.guide-page`/`.guide-card`(좌측 accent 컬러 카드) 레이아웃으로 재구성(CSS는
`styles.css`의 "퀴즈 만들기 안내/공지사항 상세 전용" 구역), `NoticesPanel.jsx` 상세 화면도 동일
`.guide-card`로 감쌈.

## 화면 이동 그래프 (요약)

```
(/퀴즈) → SelectUIModeUI ─┬─ MainUI(디스코드 트랙) ─┬─ SelectQuizTypeUI ─┬─ DevQuizSelectUI ──(트리 재귀)──┬─ DevQuizInfoUI
                          │                        │                    │                                 └─ DevQuizSelectUI(하위)
                          │                        │                    ├─ UserQuizSelectUI(브라우징) ─── UserQuizInfoUI ── UserQuestionInfoUI
                          │                        │                    └─ OmakaseQuizRoomUI ── (바구니모드) UserQuizSelectUI(담기모드)
                          │                        └─ ServerSettingUI
                          └─ WebHandoffUI(웹 트랙 — MainUI/SelectQuizTypeUI 건너뜀, mode는 요청별 분기) ─┬─ DevQuizInfoUI
                                                                                                          ├─ UserQuizInfoUI
                                                                                                          ├─ OmakaseQuizRoomUI
                                                                                                          └─ MultiplayerQuizLobbyUI(Phase 4 - 밴/음성채널 체크 통과 시)

MultiplayerQuizSelectUI ─┬─ MultiplayerQuizLobbyUI ── (바구니모드) UserQuizSelectUI(담기모드)
                         └─ ScoreboardUI

(/퀴즈만들기, DM 전용) → QuizEditSelectUIModeUI ─┬─ UserQuizListUI(디스코드 트랙, 기존과 100% 동일) ── UserQuizInfoUI ── UserQuestionInfoUI
                                                └─ QuizEditWebHandoffUI(웹 트랙, Phase 1 - 잠금 화면만, 편집 CRUD는 Phase 3~4)

QuizInfoUI 계열(Dev/Omakase/MultiplayerLobby 공통) 'start' → AlertQuizStartUI
NoteSelectUI → NoteUI
AdminPanelUI ─┬─ AdminBanListUI
              ├─ (신고처리, sendReportLog 직접 호출)
              ├─ UserQuizListUI(show_all_quizzes=true)
              ├─ AdminNoticeListUI ── AdminNoticeDetailUI
              ├─ AdminMaintenanceUI
              ├─ AdminSeasonUI
              └─ AdminLobbyListUI ── AdminLobbyDetailUI
```

**웹 포팅(2026-08-15)**: 순위표(`ScoreboardUI`)는 위 `select-ui-mode-ui.js` 트랙 분기와 무관하게
"☰ 더보기" 드롭다운에 별도로 노출됨(`web_express_app.ts`의 `GET /api/scoreboard[?season_id=]`+
`GET /api/scoreboard/seasons`, `web-frontend/src/ScoreboardPanel.jsx`) — 나머지 화면 웹 포팅
(위 섹션)과 동일한 "☰ 더보기" 자리를 재사용하되 별도 계획서(`docs/plans/SCOREBOARD_SEASON_PLAN.md`)로
추가됨.
